{
  "overview": "A simple, secure Task Management MVP using Next.js 14 (App Router), TypeScript, Tailwind CSS, and Supabase for a fully managed backend (Auth, Database). The architecture is a modern server/client component split, leveraging Server Actions for secure data mutation and client components for interactive UI elements. Security is implemented via Supabase Row Level Security (RLS) and Next.js middleware for session management.",
  "database_schema": "CREATE EXTENSION IF NOT EXISTS \"uuid-ossp\";\n\n-- Users Table (Managed by Supabase Auth, but we define a profile for application data)\nCREATE TABLE IF NOT EXISTS public.profiles (\n    id UUID PRIMARY KEY REFERENCES auth.users(id) ON DELETE CASCADE,\n    full_name TEXT NOT NULL DEFAULT '',\n    updated_at TIMESTAMP WITH TIME ZONE\n);\n\n-- Projects Table\nCREATE TABLE IF NOT EXISTS public.projects (\n    id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),\n    name VARCHAR(255) NOT NULL,\n    description TEXT,\n    owner_id UUID NOT NULL REFERENCES public.profiles(id) ON DELETE CASCADE,\n    created_at TIMESTAMP WITH TIME ZONE DEFAULT CURRENT_TIMESTAMP,\n    updated_at TIMESTAMP WITH TIME ZONE DEFAULT CURRENT_TIMESTAMP\n);\n\nCREATE INDEX idx_projects_owner_id ON public.projects(owner_id);\n\n-- Tasks Table\nCREATE TABLE IF NOT EXISTS public.tasks (\n    id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),\n    title VARCHAR(255) NOT NULL,\n    description TEXT,\n    status VARCHAR(50) NOT NULL CHECK (status IN ('TODO', 'IN_PROGRESS', 'DONE')) DEFAULT 'TODO',\n    priority VARCHAR(50) NOT NULL CHECK (priority IN ('LOW', 'MEDIUM', 'HIGH')) DEFAULT 'MEDIUM',\n    project_id UUID NOT NULL REFERENCES public.projects(id) ON DELETE CASCADE,\n    assigned_to UUID REFERENCES public.profiles(id) ON DELETE SET NULL,\n    due_date TIMESTAMP WITH TIME ZONE,\n    created_at TIMESTAMP WITH TIME ZONE DEFAULT CURRENT_TIMESTAMP,\n    updated_at TIMESTAMP WITH TIME ZONE DEFAULT CURRENT_TIMESTAMP\n);\n\nCREATE INDEX idx_tasks_project_id ON public.tasks(project_id);\nCREATE INDEX idx_tasks_assigned_to ON public.tasks(assigned_to);\n\n-- RLS Policies for Task Management (Assuming users can only see/manage projects they own or are assigned tasks in)\nALTER TABLE public.projects ENABLE ROW LEVEL SECURITY;\nALTER TABLE public.tasks ENABLE ROW LEVEL SECURITY;\n\n-- Policy for Projects: Only the owner can view/update/delete their projects\nCREATE POLICY \"Owners can manage their projects\" ON public.projects\nFOR ALL\nTO authenticated\nUSING (owner_id = auth.uid())\nWITH CHECK (owner_id = auth.uid());\n\n-- Policy for Tasks: Users can see tasks assigned to them, or tasks within projects they own.\nCREATE POLICY \"Users can see assigned tasks\" ON public.tasks\nFOR SELECT\nTO authenticated\nUSING (\n    assigned_to = auth.uid() OR\n    project_id IN (SELECT id FROM public.projects WHERE owner_id = auth.uid())\n);\n\nCREATE POLICY \"Users can update their assigned tasks\" ON public.tasks\nFOR UPDATE\nTO authenticated\nUSING (\n    assigned_to = auth.uid() OR\n    project_id IN (SELECT id FROM public.projects WHERE owner_id = auth.uid())\n);\n\n-- Policy for Task Creation: Only allowed if the user owns the project or is part of a shared project (simplified here: only project owner can create tasks initially)\nCREATE POLICY \"Owners can create tasks in their projects\" ON public.tasks\nFOR INSERT\nTO authenticated\nWITH CHECK (project_id IN (SELECT id FROM public.projects WHERE owner_id = auth.uid()));",
  "api_specification": "This MVP uses Next.js Server Actions (via src/actions/taskActions) instead of traditional REST endpoints for secure data mutation, leveraging Supabase RLS for authorization. Read operations are performed directly in Server Components or via client-side fetches using Supabase client SDK.\n\n**Data Models:**\n- **Project:** { id: string, name: string, description: string | null, owner_id: string, created_at: string }\n- **Task:** { id: string, title: string, description: string | null, status: 'TODO' | 'IN_PROGRESS' | 'DONE', priority: 'LOW' | 'MEDIUM' | 'HIGH', project_id: string, assigned_to: string | null, due_date: string | null, created_at: string }\n\n**Server Actions (src/actions/taskActions):**\n1. `createProject(name: string, description: string): Promise<{ success: boolean, error?: string, project?: Project }>`\n2. `getProjects(): Promise<{ success: boolean, projects: Project[] }>` (Reads data)\n3. `createTask(projectId: string, title: string, priority: string): Promise<{ success: boolean, error?: string, task?: Task }>`\n4. `updateTaskStatus(taskId: string, status: 'TODO' | 'IN_PROGRESS' | 'DONE'): Promise<{ success: boolean, error?: string }>`\n5. `getTasksByProject(projectId: string): Promise<{ success: boolean, tasks: Task[] }>` (Reads data)",
  "documentation": "# SmartBuilder Task Management MVP Documentation\n\n## 1. Architecture Overview\nThis application uses a T3-like stack: Next.js 14 (App Router), TypeScript, Tailwind CSS, and Supabase (PostgreSQL + Auth). \n\n**Data Flow:**\n- **Server Components (e.g., /dashboard):** Fetch data directly from Supabase using the server-side Supabase client, leveraging database indexes and RLS for security.\n- **Server Actions (src/actions/*):** Handle all data mutations (Create, Update, Delete). They use the Supabase service role or authenticated client context securely, relying on database RLS policies to enforce business logic and authorization.\n- **Client Components:** Handle interactivity (forms, state management via Zustand).\n\n**Security:** Security is primarily enforced via PostgreSQL Row Level Security (RLS) defined in the schema. Authentication is handled by Supabase Auth, and session management is implicitly handled by Next.js middleware/Supabase helpers.\n\n## 2. API & Data Access\nData access is decoupled from traditional REST endpoints and implemented via **Server Actions** located in `src/actions/`. This pattern prevents accidental exposure of sensitive logic or direct database access from the client.\n\n## 3. Development Guide\n1. **Prerequisites:** Node.js (v18+), npm/yarn/pnpm.\n2. **Setup:**\n   a. Create a Supabase project and configure the connection details in `.env.local`.\n   b. Apply the provided PostgreSQL schema to your Supabase database.\n3. **Running Locally:**\n   `npm run dev`\n4. **State Management:** Zustand store (`src/store/useTaskStore.ts`) manages global UI state (e.g., loading indicators, modals).\n\n## 4. Deployment Guide\nThis application is designed for deployment on Vercel.\n1. **Environment Variables:** Ensure `.env.local` contains:\n   ```\n   NEXT_PUBLIC_SUPABASE_URL=your_url\n   NEXT_PUBLIC_SUPABASE_ANON_KEY=your_anon_key\n   SUPABASE_SERVICE_KEY=your_service_role_key\n   ```\n2. **CI/CD:** A GitHub Actions workflow (`.github/workflows/ci.yml`) is configured to run tests and build on every push to main.\n3. **Database Migration:** The schema must be applied manually or via a migration script before deployment.",
  "files": [
    {
      "path": "package.json",
      "content": "{\n  \"name\": \"smartbuilder-task-manager\",\n  \"version\": \"0.1.0\",\n  \"private\": true,\n  \"scripts\": {\n    \"dev\": \"next dev\",\n    \"build\": \"next build\",\n    \"start\": \"next start\",\n    \"lint\": \"next lint\",\n    \"test\": \"jest --config=jest.config.js\"\n  },\n  \"dependencies\": {\n    \"@supabase/auth-helpers-nextjs\": \"^0.9.0\",\n    \"@supabase/ssr\": \"^0.1.0\",\n    \"@supabase/supabase-js\": \"^2.42.0\",\n    \"next\": \"14.2.3\",\n    \"react\": \"^18\",\n    \"react-dom\": \"^18\",\n    \"zustand\": \"^4.5.2\"\n  },\n  \"devDependencies\": {\n    \"@types/node\": \"^20\",\n    \"@types/react\": \"^18\",\n    \"@types/react-dom\": \"^18\",\n    \"autoprefixer\": \"^10.0.1\",\n    \"eslint\": \"^8\",\n    \"eslint-config-next\": \"14.2.3\",\n    \"postcss\": \"^8\",\n    \"tailwindcss\": \"^3.3.0\",\n    \"typescript\": \"^5\",\n    \"@testing-library/react\": \"^15.0.7\",\n    \"jest\": \"^29.7.0\",\n    \"ts-jest\": \"^29.1.2\"\n  }\n}"
    },
    {
      "path": "src/lib/supabase/server.ts",
      "content": "import { createServerClient, type CookieOptions } from '@supabase/ssr';\nimport { cookies } from 'next/headers';\nimport { Database } from '@/types/supabase';\n\n// Server-side Supabase Client for Server Components and Server Actions\nexport const createServerSupabaseClient = () => {\n  const cookieStore = cookies();\n\n  return createServerClient<Database>(\n    process.env.NEXT_PUBLIC_SUPABASE_URL!,\n    process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!,\n    {\n      cookies: {\n        get(name: string) {\n          return cookieStore.get(name)?.value;\n        },\n        set(name: string, value: string, options: CookieOptions) {\n          // For SSR/Server Actions, we don't write cookies back, only read them.\n        },\n        remove(name: string, options: CookieOptions) {\n          // Do nothing\n        },\n      },\n    }\n  );\n};\n\n// Server-side Supabase Client with Service Role Key for administrative tasks (used sparingly in Server Actions)\nexport const createServiceSupabaseClient = () => {\n    // WARNING: Service Role Key bypasses RLS. Use ONLY for internal setup or administrative tasks.\n    return createServerClient<Database>(\n        process.env.NEXT_PUBLIC_SUPABASE_URL!,\n        process.env.SUPABASE_SERVICE_KEY!,\n        {\n            cookies: {},\n        }\n    );\n};\n"
    },
    {
      "path": "src/lib/supabase/client.ts",
      "content": "import { createClientComponentClient } from '@supabase/auth-helpers-nextjs';\nimport { Database } from '@/types/supabase';\n\n// Client-side Supabase Client for Client Components\nexport const createClientSupabaseClient = () => {\n  return createClientComponentClient<Database>();\n};\n"
    },
    {
      "path": "src/types/database.types.ts",
      "content": "export type Json =\n  | null\n  | boolean\n  | number\n  | string\n  | string[]\n  | { [key: string]: Json | undefined }\n  | { [key: string]: Json }\n\nexport type Database = {\n  public:\n    {\n      Tables:\n        {\n          profiles:\n            {\n              Row: {\n                id: string\n                full_name: string\n                updated_at: string | null\n              }\n              Insert: {\n                id: string\n                full_name?: string\n                updated_at?: string | null\n              }\n              Update: {\n                id: string\n                full_name?: string\n                updated_at?: string | null\n              }\n              Relationships: []\n            }\n          projects:\n            {\n              Row:\n                {\n                  id: string\n                  name: string\n                  description: string | null\n                  owner_id: string\n                  created_at: string\n                  updated_at: string\n                }\n              Insert: {\n                id?: string\n                name: string\n                description?: string | null\n                owner_id: string\n                created_at?: string\n                updated_at?: string | null\n              }\n              Update: {\n                id?: string\n                name?: string\n                description?: string | null\n                owner_id?: string\n                created_at?: string\n                updated_at?: string | null\n              }\n              Relationships: [\n                {\n                  to_table: 'profiles'\n                  composite: false\n                  foreign_key: 'owner_id'\n                  primary_key: 'id'\n                }\n              ]\n            }\n          tasks:\n            {\n              Row:\n                {\n                  id: string\n                  title: string\n                  description: string | null\n                  status: 'TODO' | 'IN_PROGRESS' | 'DONE'\n                  priority: 'LOW' | 'MEDIUM' | 'HIGH'\n                  project_id: string\n                  assigned_to: string | null\n                  due_date: string | null\n                  created_at: string\n                  updated_at: string\n                }\n              Insert:\n                {\n                  id?: string\n                  title: string\n                  description?: string | null\n                  status?: 'TODO' | 'IN_PROGRESS' | 'DONE'\n                  priority?: 'LOW' | 'MEDIUM' | 'HIGH'\n                  project_id: string\n                  assigned_to?: string | null\n                  due_date?: string | null\n                  created_at?: string\n                  updated_at?: string | null\n                }\n              Update:\n                {\n                  id?: string\n                  title?: string\n                  description?: string | null\n                  status?: 'TODO' | 'IN_PROGRESS' | 'DONE'\n                  priority?: 'LOW' | 'MEDIUM' | 'HIGH'\n                  project_id?: string\n                  assigned_to?: string | null\n                  due_date?: string | null\n                  created_at?: string\n                  updated_at?: string | null\n                }\n              Relationships: [\n                {\n                  to_table: 'projects'\n                  composite: false\n                  foreign_key: 'project_id'\n                  primary_key: 'id'\n                },\n                {\n                  to_table: 'profiles'\n                  composite: false\n                  foreign_key: 'assigned_to'\n                  primary_key: 'id'\n                }\n              ]\n            }\n        }\n      Views: {}\n      Functions: {}\n      Enums: {}\n    }\n  }\n"
    },
    {
      "path": "src/types/models.ts",
      "content": "import { Database } from './database.types';\n\nexport type Profile = Database['public']['Tables']['profiles']['Row'];\nexport type Project = Database['public']['Tables']['projects']['Row'];\nexport type Task = Database['public']['Tables']['tasks']['Row'] & {\n  profiles: Profile | null;\n};\n\nexport type TaskStatus = 'TODO' | 'IN_PROGRESS' | 'DONE';\nexport type TaskPriority = 'LOW' | 'MEDIUM' | 'HIGH';\n\nexport interface ActionResponse {\n    success: boolean;\n    message: string;\n    data?: any;\n    error?: string;\n}\n"
    },
    {
      "path": "src/actions/taskActions.ts",
      "content": "'use server';\n\nimport { revalidatePath } from 'next/cache';\nimport { createServerSupabaseClient } from '@/lib/supabase/server';\nimport { Project, Task, ActionResponse } from '@/types/models';\nimport { Database } from '@/types/database.types';\n\n// --- Utility Functions ---\n\nconst getUserId = async (): Promise<string> => {\n    const supabase = createServerSupabaseClient();\n    const { data: { user } } = await supabase.auth.getUser();\n    if (!user) {\n        throw new Error('Authentication required.');\n    }\n    return user.id;\n};\n\n// --- Project Actions ---\n\nexport async function createProject(formData: FormData): Promise<ActionResponse> {\n    try {\n        const name = formData.get('name') as string;\n        const description = formData.get('description') as string;\n        const owner_id = await getUserId();\n\n        if (!name) {\n            return { success: false, message: 'Project name is required.' };\n        }\n\n        const supabase = createServerSupabaseClient<Database>();\n\n        const { data, error } = await supabase\n            .from('projects')\n            .insert([{ name, description, owner_id }])\n            .select()\n            .single();\n\n        if (error) {\n            console.error(\"Project creation error:\", error);\n            throw new Error(error.message || 'Failed to create project.');\n        }\n\n        revalidatePath('/'); // Refresh dashboard data\n        return { success: true, message: 'Project created successfully.', data: data as Project };\n\n    } catch (e) {\n        const message = e instanceof Error ? e.message : 'An unknown error occurred during project creation.';\n        return { success: false, message: 'Error', error: message };\n    }\n}\n\nexport async function getProjects(): Promise<{ success: boolean, projects: Project[], error?: string }> {\n    try {\n        const owner_id = await getUserId();\n        const supabase = createServerSupabaseClient<Database>();\n\n        const { data, error } = await supabase\n            .from('projects')\n            .select('*')\n            .eq('owner_id', owner_id)\n            .order('created_at', { ascending: false });\n\n        if (error) {\n            throw new Error(error.message || 'Failed to fetch projects.');\n        }\n\n        return { success: true, projects: data as Project[] };\n\n    } catch (e) {\n        const message = e instanceof Error ? e.message : 'An unknown error occurred.';\n        return { success: false, projects: [], error: message };\n    }\n}\n\n// --- Task Actions ---\n\nexport async function getTasksByProject(projectId: string): Promise<{ success: boolean, tasks: Task[], error?: string }> {\n    try {\n        const supabase = createServerSupabaseClient<Database>();\n\n        const { data, error } = await supabase\n            .from('tasks')\n            .select('*, profiles(full_name)') // Join with profiles to get assignee name\n            .eq('project_id', projectId)\n            .order('priority', { ascending: false })\n            .order('created_at', { ascending: true });\n\n        if (error) {\n            throw new Error(error.message || 'Failed to fetch tasks.');\n        }\n\n        return { success: true, tasks: data as Task[] };\n\n    } catch (e) {\n        const message = e instanceof Error ? e.message : 'An unknown error occurred.';\n        return { success: false, tasks: [], error: message };\n    }\n}\n\nexport async function createTask(formData: FormData): Promise<ActionResponse> {\n    try {\n        const title = formData.get('title') as string;\n        const priority = formData.get('priority') as string;\n        const projectId = formData.get('projectId') as string;\n\n        if (!title || !priority || !projectId) {\n            return { success: false, message: 'Missing required task fields.' };\n        }\n\n        const supabase = createServerSupabaseClient<Database>();\n        \n        // Note: Assignment logic is omitted for simplicity in this MVP, tasks are unassigned initially\n        const { data, error } = await supabase\n            .from('tasks')\n            .insert([{ title, priority, project_id: projectId, status: 'TODO' }])\n            .select('*, profiles(full_name)')\n            .single();\n\n        if (error) {\n            console.error(\"Task creation error:\", error);\n            throw new Error(error.message || 'Failed to create task.');\n        }\n\n        revalidatePath(`/project/${projectId}`); // Refresh task list\n        return { success: true, message: 'Task created successfully.', data: data as Task };\n\n    } catch (e) {\n        const message = e instanceof Error ? e.message : 'An unknown error occurred.';\n        return { success: false, message: 'Error', error: message };\n    }\n}\n\nexport async function updateTaskStatus(taskId: string, status: Task['status']): Promise<ActionResponse> {\n    try {\n        const supabase = createServerSupabaseClient<Database>();\n\n        const { error } = await supabase\n            .from('tasks')\n            .update({ status, updated_at: new Date().toISOString() })\n            .eq('id', taskId)\n            .select();\n\n        if (error) {\n            throw new Error(error.message || 'Failed to update task status.');\n        }\n\n        revalidatePath('/', { scope: 'all' }); // Invalidate all paths that might display this task\n        return { success: true, message: 'Task status updated.' };\n\n    } catch (e) {\n        const message = e instanceof Error ? e.message : 'An unknown error occurred.';\n        return { success: false, message: 'Error', error: message };\n    }\n}\n"
    },
    {
      "path": "src/store/useTaskStore.ts",
      "content": "import { create } from 'zustand';\nimport { Project, Task } from '@/types/models';\n\ninterface TaskState {\n  isProjectModalOpen: boolean;\n  toggleProjectModal: () => void;\n  currentProjects: Project[];\n  setProjects: (projects: Project[]) => void;\n  loadingProjects: boolean;\n  setLoadingProjects: (loading: boolean) => void;\n}\n\nexport const useTaskStore = create<TaskState>((set) => ({\n  isProjectModalOpen: false,\n  toggleProjectModal: () => set((state) => ({ isProjectModalOpen: !state.isProjectModalOpen })),\n  currentProjects: [],\n  setProjects: (projects) => set({ currentProjects: projects }),\n  loadingProjects: false,\n  setLoadingProjects: (loading) => set({ loadingProjects: loading }),\n}));\n"
    },
    {
      "path": "src/components/layout/Navbar.tsx",
      "content": "import Link from 'next/link';\nimport { createServerSupabaseClient } from '@/lib/supabase/server';\nimport { Button } from '@/components/ui/Button';\nimport { Database } from '@/types/database.types';\n\n// Helper to fetch user profile on the server\nasync function getUserProfile() {\n    const supabase = createServerSupabaseClient<Database>();\n    const { data: { user } } = await supabase.auth.getUser();\n\n    if (!user) return null;\n\n    const { data: profile } = await supabase\n        .from('profiles')\n        .select('full_name')\n        .eq('id', user.id)\n        .single();\n\n    return { email: user.email, name: profile?.full_name || 'User' };\n}\n\nexport async function Navbar() {\n    const userProfile = await getUserProfile();\n\n    return (\n        <header className=\"sticky top-0 z-50 w-full border-b bg-white/80 backdrop-blur-sm\">\n            <div className=\"container flex h-16 items-center justify-between px-4 sm:px-6 lg:px-8\">\n                <Link href=\"/\" className=\"text-xl font-bold text-indigo-600\">\n                    TaskFlow\n                </Link>\n                <nav className=\"flex items-center space-x-4\">\n                    {userProfile ? (\n                        <div className=\"flex items-center space-x-3\">\n                            <span className=\"text-sm text-gray-600 hidden sm:inline\">Hello, {userProfile.name}</span>\n                            <form action=\"/auth/logout\" method=\"post\">\n                                <Button type=\"submit\" variant=\"outline\" size=\"sm\">\n                                    Logout\n                                </Button>\n                            </form>\n                        </div>\n                    ) : (\n                        <Link href=\"/login\">\n                            <Button variant=\"default\" size=\"sm\">\n                                Sign In\n                            </Button>\n                        </Link>\n                    )}\n                </nav>\n            </div>\n        </header>\n    );\n}\n"
    },
    {
      "path": "src/app/layout.tsx",
      "content": "import type { Metadata } from \"next\";\nimport { Inter } from \"next/font/google\";\nimport { Navbar } from '@/components/layout/Navbar';\nimport { TailwindProvider } from '@/components/providers/TailwindProvider';\nimport { AuthProvider } from '@/components/providers/AuthProvider';\nimport \"./globals.css\";\n\nconst inter = Inter({ subsets: [\"latin\"] });\n\nexport const metadata: Metadata = {\n  title: \"TaskFlow - Simple Task Manager\",\n  description: \"A modern, secure task management MVP built with Next.js and Supabase.\",\n};\n\nexport default function RootLayout({ children }: { children: React.ReactNode }) {\n  return (\n    <html lang=\"en\">\n      <body className={inter.className}>\n        <TailwindProvider>\n          <AuthProvider>\n            <Navbar />\n            <main className=\"container mx-auto px-4 py-8 sm:px-6 lg:px-8\">\n                {children}\n            </main>\n          </AuthProvider>\n        </TailwindProvider>\n      </body>\n    </html>\n  );\n}\n"
    },
    {
      "path": "src/app/page.tsx",
      "content": "import { redirect } from 'next/navigation';\nimport { createServerSupabaseClient } from '@/lib/supabase/server';\nimport Dashboard from '@/components/pages/Dashboard';\nimport { Database } from '@/types/database.types';\n\nexport default async function HomePage() {\n    const supabase = createServerSupabaseClient<Database>();\n    const { data: { session } } = await supabase.auth.getSession();\n\n    if (!session) {\n        redirect('/login');\n    }\n\n    // Ensure profile exists (essential for RLS)\n    const { data: profile } = await supabase\n        .from('profiles')\n        .select('id')\n        .eq('id', session.user.id)\n        .single();\n\n    if (!profile) {\n        // In a real app, this would trigger profile creation flow\n        return (\n            <div className=\"text-center p-10\">\n                <h1 className=\"text-2xl font-bold mb-4\">Profile Missing</h1>\n                <p>Please complete your profile setup. (Simulated redirect to login for MVP)</p>\n                <form action=\"/auth/logout\" method=\"post\">\n                    <button type=\"submit\" className=\"mt-4 text-indigo-600 hover:text-indigo-800\">Logout</button>\n                </form>\n            </div>\n        );\n    }\n\n    return <Dashboard />;\n}\n"
    },
    {
      "path": "src/components/pages/Dashboard.tsx",
      "content": "'use client';\n\nimport { useEffect, useState } from 'react';\nimport { useTaskStore } from '@/store/useTaskStore';\nimport { getProjects, createProject } from '@/actions/taskActions';\nimport { Project, Task } from '@/types/models';\nimport ProjectList from '@/components/dashboard/ProjectList';\nimport ProjectDetail from '@/components/dashboard/ProjectDetail';\nimport { Button } from '@/components/ui/Button';\nimport { Modal } from '@/components/ui/Modal';\nimport { Input } from '@/components/ui/Input';\nimport { Textarea } from '@/components/ui/Textarea';\nimport { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/Card';\nimport { Spinner } from '@/components/ui/Spinner';\n\n// --- Project Creation Form Component ---\nconst NewProjectForm = () => {\n    const { toggleProjectModal } = useTaskStore();\n    const [title, setTitle] = useState('');\n    const [description, setDescription] = useState('');\n    const [loading, setLoading] = useState(false);\n    const [error, setError] = useState('');\n\n    const handleSubmit = async (e: React.FormEvent) => {\n        e.preventDefault();\n        if (!title.trim()) {\n            setError('Project title cannot be empty.');\n            return;\n        }\n\n        setLoading(true);\n        setError('');\n\n        const formData = new FormData();\n        formData.append('name', title);\n        formData.append('description', description);\n\n        const response = await createProject(formData);\n\n        setLoading(false);\n\n        if (response.success) {\n            toggleProjectModal();\n        } else {\n            setError(response.error || 'Failed to create project.');\n        }\n    };\n\n    return (\n        <form onSubmit={handleSubmit} className=\"space-y-4\">\n            <h3 className=\"text-xl font-semibold\">Create New Project</h3>\n            <div>\n                <label htmlFor=\"new-project-title\" className=\"block text-sm font-medium text-gray-700\">Title</label>\n                <Input\n                    id=\"new-project-title\"\n                    value={title}\n                    onChange={(e) => setTitle(e.target.value)}\n                    required\n                    placeholder=\"Project Alpha\"\n                    disabled={loading}\n                />\n            </div>\n            <div>\n                <label htmlFor=\"new-project-desc\" className=\"block text-sm font-medium text-gray-700\">Description (Optional)</label>\n                <Textarea\n                    id=\"new-project-desc\"\n                    value={description}\n                    onChange={(e) => setDescription(e.target.value)}\n                    rows={3}\n                    disabled={loading}\n                />\n            </div>\n            {error && <p className=\"text-sm text-red-600\">{error}</p>}\n            <div className=\"flex justify-end space-x-2 pt-2\">\n                <Button type=\"button\" variant=\"outline\" onClick={toggleProjectModal} disabled={loading}>Cancel</Button>\n                <Button type=\"submit\" disabled={loading || !title.trim()}>\n                    {loading ? <Spinner className='w-4 h-4 mr-2' /> : 'Create Project'}\n                </Button>\n            </div>\n        </form>\n    );\n};\n\n// --- Main Dashboard Component ---\nconst Dashboard = () => {\n    const { isProjectModalOpen, toggleProjectModal, currentProjects, setProjects, loadingProjects, setLoadingProjects } = useTaskStore();\n    const [selectedProjectId, setSelectedProjectId] = useState<string | null>(null);\n\n    const loadProjects = async () => {\n        setLoadingProjects(true);\n        const result = await getProjects();\n        if (result.success) {\n            setProjects(result.projects);\n            if (result.projects.length > 0 && !selectedProjectId) {\n                setSelectedProjectId(result.projects[0].id);\n            }\n        } else {\n            console.error(\"Failed to load projects:\", result.error);\n        }\n        setLoadingProjects(false);\n    };\n\n    useEffect(() => {\n        loadProjects();\n    // eslint-disable-next-line react-hooks/exhaustive-deps\n    }, []); \n\n    // Auto-select first project if the list changes and the current selection is invalid\n    useEffect(() => {\n        if (currentProjects.length > 0 && !selectedProjectId) {\n            setSelectedProjectId(currentProjects[0].id);\n        } else if (currentProjects.length === 0) {\n            setSelectedProjectId(null);\n        }\n    }, [currentProjects, selectedProjectId]);\n\n    const selectedProject = currentProjects.find(p => p.id === selectedProjectId);\n\n    return (\n        <div className=\"space-y-6\">\n            <header className=\"flex justify-between items-center\">\n                <h1 className=\"text-3xl font-bold text-gray-800\">Your Workspace</h1>\n                <Button onClick={toggleProjectModal} disabled={loadingProjects}>\n                    + New Project\n                </Button>\n            </header>\n\n            {loadingProjects ? (\n                <div className=\"flex justify-center items-center h-64\">\n                    <Spinner className='w-8 h-8 text-indigo-500' />\n                    <p className='ml-3 text-gray-600'>Loading projects...</p>\n                </div>\n            ) : currentProjects.length === 0 ? (\n                <Card className='p-10 text-center border-dashed'>\n                    <CardTitle className='mb-2'>No Projects Found</CardTitle>\n                    <CardContent className='text-gray-500'>Get started by creating your first project!</CardContent>\n                    <Button onClick={toggleProjectModal} className='mt-4'>Create First Project</Button>\n                </Card>\n            ) : (\n                <div className=\"grid grid-cols-1 lg:grid-cols-4 gap-6\">\n                    {/* Project List Sidebar */}\n                    <div className=\"lg:col-span-1\">\n                        <ProjectList \n                            projects={currentProjects}\n                            selectedId={selectedProjectId}\n                            onSelect={setSelectedProjectId}\n                        />\n                    </div>\n                    {/* Project Detail View */}\n